package interface_adapter;

public interface Presenter {
    // define the methods for the presenter
    void displayMessage(String message);
    void displayOptions();
    void displayFeedback(int score);
}